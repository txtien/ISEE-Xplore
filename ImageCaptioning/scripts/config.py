from pathlib import Path

root = Path.cwd().parent

RAW_IMAGE_PATH = root / 'data' / 'raw' / 'images' 
RAW_TEXT_PATH = root / 'data' / 'raw' / 'text'

PREPROCESS_IMAGE_PATH = root / 'data' / 'preprocess' / 'images' 
PREPROCESS_TEXT_PATH = root / 'data' / 'preprocess' / 'text' 